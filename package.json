{
  "name": "birdle",
  "private": true,
  "version": "0.1.0",
  "description": "鹬一把 —— 猜北京地区鸟类的 Wordle 式游戏",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "start": "pnpm --filter server run start"
  },
  "engines": {
    "node": ">=22"
  }
}
